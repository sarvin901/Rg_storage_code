name: Terraform CI/CD
on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    branches:
      - main
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
